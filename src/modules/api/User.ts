// import axios from 'axios'
// import { apiBase } from 'config'

const UserEndpoints = {
  // login: credentials =>
  //   axios.post(`${apiBase}/user/login`, credentials).then(res => res.data)
}

export default UserEndpoints
